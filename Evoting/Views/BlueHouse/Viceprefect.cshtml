@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        h1 {
            text-align: center;
            margin-bottom: -1px;
            padding-bottom: 1px;
            font-size: 70px;
            font-family: "montserrat";
        }

        .button-container {
            text-align: center;
            margin-top: 10px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .btn {
            width: 200px;
            height: 200px;
            border: 1px solid #000000;
            background: none;
            padding: 10px 20px;
            font-size: 40px;
            font-family: "montserrat";
            cursor: pointer;
            margin: 20px;
            border-radius: 25px;
        }

        .blue {
            background-color: #0451c4;
            color: white;
        }

            .blue:hover {
                background-color: #2f5beb;
                border-color: #2f5beb;
            }

        .house-button:hover {
            color: #ddd;
        }





      



        /* body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            text-align: center;
            margin: 50px;
        }

        h1 {
            color: #333;
        }

        .candidates {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }

        .candidate-button {
            width: 200px;
            height: 200px;
            border: 1px solid #000000;
            background: none;
            padding: 10px 20px;
            font-size: 40px;
            font-family: "montserrat";
            cursor: pointer;
            margin: 20px;
            border-radius: 25px;
            position: relative;
            overflow: hidden;
            border: none;
            cursor: pointer;
            transition: transform 0.3s ease-in-out;
        }

            .candidate-button img {
                width: 100%;
                height: 100%;
                object-fit: cover;
            }

            .candidate-button span {
                position: absolute;
                bottom: 0;
                left: 0;
                width: 100%;
                background-color: none;
                color: black;
                padding: 10px;
                box-sizing: border-box;
            }

            .candidate-button:hover {
                transform: scale(1.05);
            } */
    </style>
    <title>Captain Election</title>
</head>
<body>
    <h1>Vote for School Vice Prefect</h1>

    <div class="button-container">
        @*  <div class="candidates"> *@
        <button class="btn blue" id="blue-house" @* class="candidate-button" id="can" *@ onclick="vote('Krishna Gurung')">
            <img><span>Krishna Gurung</span>
        </button>
        <button class="btn blue" id="blue-house" @* class="candidate-button" id="can" *@ onclick="vote('Anish Thapa')">
            <img><span>Anish Thapa</span>
        </button>
    </div>

    <div class="button-container">
        @*  <div class="candidates"> *@
        <button class="btn blue" id="blue-house" @* class="candidate-button" id="can" *@ onclick="vote('Pabitra Dhakal')">
            <img><span>Pabitra Dhakal</span>
        </button>
        <button class="btn blue" id="blue-house" @* class="candidate-button" id="can" *@ onclick="vote('Anupama Giri')">
            <img><span>Anupama Giri</span>
        </button>
    </div>

    <script>
        function vote(candidate) {
            //alert('You voted for ' + candidate);

            $.ajax({
                url: "@Url.Action("CreateVote", "BlueHouse")",
                method: "POST",
                data: {
                    candidate: candidate,
                    candidateType: "Viceprefect"
                },
                success: function () {
                    window.location.href = "@Url.Action("Housecaption", "BlueHouse")";
                }
            })
            // Add your logic to record the vote
        }

        document.getElementById('c').addEventListener('click', function () {
            window.location.href = 'Viceprefect.html';
        });

    </script>
    @*  <script>
        function vote(candidate) {
            alert('You voted for ' + candidate);
            // Add your logic to record the vote
        }

        document.getElementById('c').addEventListener('click', function () {
            window.location.href = 'prefect.html';
        });

    </script> *@
</body>
</html>

